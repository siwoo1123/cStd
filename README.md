# C++ project

I study C++
```c++
#include <iostream>
#define nil NULL

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nil);
    cout.tie(nil);
    
    cout << "Hello, World!!\n";
    return 0;
}
```